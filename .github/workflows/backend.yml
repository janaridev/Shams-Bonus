name: .NET

on:
  push:
    branches: ["master"]
    paths:
      - "backend/**"
  pull_request:
    branches: ["master"]
    paths:
      - "backend/**"

jobs:
  api:
    runs-on: self-hosted
    steps:
      - name: Restore dependencies
        run: cd /home/ubuntu/Shams-Bonus/_work/Shams-Bonus/Shams-Bonus/backend && sudo dotnet restore
      - name: Build
        run: cd /home/ubuntu/Shams-Bonus/_work/Shams-Bonus/Shams-Bonus/backend && sudo dotnet build --no-restore
      - name: Publish
        run: cd /home/ubuntu/Shams-Bonus/_work/Shams-Bonus/Shams-Bonus/backend && sudo dotnet publish -C release -o /home/ubuntu/production/backend
      - name: pm2 restart
        run: sudo pm2 restart backend
